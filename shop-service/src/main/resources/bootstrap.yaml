spring:
  application:
    name: shop-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.233.145:8848
      username: nacos
      password: nacos
      config:
        file-extension: yaml
        shared-configs:
          - dataId: shared-jdbc.yaml
          - dataId: shared-logback.yaml
          - dataId: shared-redis.yaml
